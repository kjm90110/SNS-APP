document.addEventListener("submit", () => {
    const id = document.getElementById("userId");
    const pw = document.getElementById("password");

    const auth = /^[a-zA-Z0-9!@#$%^&*()]{8,}/; // 영대소문자, 숫자, 특수문자 포함 8자 이상
});
